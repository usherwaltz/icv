# iCV Development Environment Variables
# 
# Copy this file to .env and fill in your actual values.
# Note: .env files should NOT be committed to version control.
#
# For Flutter (Client) Environment Variables:
# These are passed via --dart-define when running Flutter apps
# Example: flutter run --dart-define=SERVER_URL=http://localhost:8080/

# Serverpod Server URL
# When running on a physical device, use your computer's IP address
# Find your IP with: ifconfig (Mac/Linux) or ipconfig (Windows)
SERVER_URL=http://localhost:8080/

# For Serverpod Server Environment Variables:
# Most configuration is in icv_server/config/development.yaml and passwords.yaml
# These can also be set as environment variables with SERVERPOD_ prefix

# Serverpod Run Mode (development, staging, production, test)
SERVERPOD_RUN_MODE=development

# Serverpod Server Role (monolith, serverless, maintenance)
SERVERPOD_SERVER_ROLE=monolith

# Database Configuration (usually set in config/development.yaml)
# SERVERPOD_DATABASE_HOST=localhost
# SERVERPOD_DATABASE_PORT=8090
# SERVERPOD_DATABASE_NAME=icv
# SERVERPOD_DATABASE_USER=postgres

# Custom Passwords (accessible via session.passwords or Serverpod.instance.passwords)
# Format: SERVERPOD_PASSWORD_<key>
# Example: SERVERPOD_PASSWORD_myApiKey becomes available as 'myApiKey'
